spring:
  application:
    name: yzl-product-server
  profiles:
    active: dev
logging:
  file:
    path: /tmp
  config: classpath:logback.xml
mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true